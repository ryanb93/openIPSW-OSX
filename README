All information is pulled from a database (included) which must first be imported to a MySQL server, and the connection info must be configured before compiling.
This application uses the Sparkle framework to handle updates. More information can be found here: http://sparkle.andymatuschak.org/